enum EndpointError {
  USER_RETRIEVAL = "error retrieving user",
  INVALID_INPUT = "invalid input",
  USER_NOT_SPECIFIED = 'user not specified',
  USER_NOT_FOUND = 'user not found',
  EVENT_SERVER_ERROR = 'event server error',
}

export default EndpointError;
